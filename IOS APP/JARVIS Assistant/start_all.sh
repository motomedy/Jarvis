#!/bin/bash
# This script starts both frontend and backend servers in the background.
# Replace the example commands below with your actual start commands and paths.

# Start backend server
# (Uncomment and edit the following line as needed)
# cd /path/to/backend && npm start &

# Start frontend server
# (Uncomment and edit the following line as needed)
# cd /path/to/frontend && npm start &

# Wait a moment to let servers start
sleep 2

echo "Frontend and backend servers have been started (check your processes and logs)."
